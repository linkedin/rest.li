namespace com.linkedin.restli.restspec

/**
 * Custom annotation for idl
 */
record CustomAnnotationSchema {

  /**
   * custom annotation data
   */
  annotations: optional map[string, CustomAnnotationContentSchema]
}